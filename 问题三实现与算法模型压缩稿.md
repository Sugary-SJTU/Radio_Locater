# 问题三：实现思路与算法模型压缩稿

> 对应当前代码（2026-09-12）。主策略：`integrated_bearing_tour`，默认1150m六边形、优先当前频道、小规模精确排路与末段共享补测。本文用于快速理解、论文建模与后续开发；实验结果区分本地配对测试与实际演练，不把事后位置当成在线已知信息。

## 1. 任务、约束与目标

场地为半径1800m的圆域D，20个频道中有10～16个干扰源，每频道至多一源。源位置与接收半径未知，接收半径固定且在1000～1500m内。机器人从原点、频道1开始，速度5m/s，不要求返航。

| 动作或条件 | 规则 |
|---|---|
| 测向 | 到达指定位置、检测指定频道，耗时5s |
| 换频 | 频道改变固定耗时1s，与频道编号差无关 |
| `direction` | 返回方向，测向误差界±1° |
| `no_signal` | 当前位置、当前频道未收到信号，不能直接认定源不存在 |
| `near` | 在测向位置距该频道源≤5m；不是移动途中的自动报警 |
| 清除 | 距源≤20m成功；成功耗时5s，失败耗时3s |

优化整局虚拟时间：

\[
T=\frac{L}{5}+5N_m+N_s+5N_c+3N_f,
\]

其中L为移动路程，N_m、N_s、N_c、N_f分别为测向、换频、成功清除和失败清除次数。少走1000m节省200s，因此主线是让覆盖、交会和清除共享移动，而非只减少换频。

## 2. 总体结构

**保证覆盖骨架＋多频道共享交会＋覆盖/目标联合开放巡回＋末段粗定位补测＋安全清除兜底。**

- 保留可完成全域搜索的覆盖点，防止遗漏未知频道。
- 在一个测站依次检测多个频道，使一次移动服务多个定位任务。
- 将剩余覆盖点与目标估计中心一起排路，搜索与清除交替执行。
- 粗定位源在末段不再被硬性排除，先补测、再重排。
- 规划坐标只是估计；实际清除依据几何安全条件或near响应。

## 3. 保证覆盖模型

覆盖点为原点O与半径a的正六边形顶点：

\[
P_k=a(\cos(k\pi/3),\sin(k\pi/3)),\quad k=0,\ldots,5.
\]

必须按最小接收半径1000m设计。距中心r的点与最近顶点夹角最多30°，最坏角度下到最近测站的距离为：

\[
f(r)=\min\{r,\sqrt{r^2+a^2-\sqrt3ar}\}.
\]

中心与顶点等距位置为r=a/√3。在本次半径范围内，结合顶点距离平方的凸性：

\[
d_{\max}=\max\left\{\frac a{\sqrt3},\sqrt{1800^2+a^2-\sqrt3\,1800a}\right\}.
\]

令d_max≤1000，缩小半径的下限为：

\[
a_{\min}=1800\cos30^\circ-\sqrt{1000^2-1800^2\sin^230^\circ}
\approx1122.956\text{m}.
\]

默认a=1150m，最坏距离988.511m，覆盖余量约11.49m；1130m也可覆盖，但余量仅3.05m；1100m不可覆盖。原点加顺序访问六顶点、不返航的基础路程为6a=6900m，实际联合路线不固定按此顺序走。代码使用连续解析判据，并用20m网格复核。

## 4. 状态与几何定位

每频道状态为`unknown → detected → localized → cleared`，或经完整排除后为`absent`。失败清除可使localized退回detected。共享状态层的localized阈值为20m，主策略实际清除使用更保守的19.8m。

维护两类不同集合：

1. **未知频道的未覆盖集合S_j**：用40m网格记录。无信号时排除检测位置M的1000m保证接收圆；S_j为空才标记absent。
2. **已发现频道的定位可行区域U_j**：场地区域与历史测向楔形相交。

\[
U_j=D\cap\bigcap_iW(M_i,\theta_i,1^\circ).
\]

计算包住U_j的最小覆盖圆：

\[
(c_j,\rho_j)=\arg\min_{c,\rho}\{\rho:U_j\subseteq B(c,\rho)\}.
\]

c_j用于代表位置，ρ_j描述几何不确定范围。它们不是目标真值或接收半径。代码还维护每频道480个存在性—位置—接收半径粒子；主策略排路主要依赖几何区域，不能称为完整贝叶斯最优决策。当前也未将全部阴性观测与测向约束统一为精确联合后验；几何区域以多边形近似实现。

## 5. 共享测站与频道顺序

到覆盖点时：扫描能减少S_j的未知频道，并复测满足筛选条件的detected频道。共享补测要求：

- 距该频道各历史测站至少50m；
- \(\|M-c_j\|\le1500+\rho_j\)，即尚不能排除接收到信号的可能。

该条件只过滤明显不可能接收的点，不保证此次测向有效。localized频道通常不再参加共享补测。普通清除位置只补测已发现频道；末段专设补测位置也扫描需要覆盖的未知频道。

默认`legacy`优先当前频道，再排列其他频道。可选`alternating`在每个非空共享扫描批次交替递增/递减，空批次不翻转；单目标定位内部补测不计作交替批次。频道仍逐个检测，只有移动成本共享。

## 6. 联合开放路径规划

设当前位置x，剩余覆盖节点集合P，符合条件的目标中心集合C，规划节点V=P∪C。初期只有ρ_j≤150m的已发现目标入队；默认probe模式在剩余覆盖节点≤2时放宽为所有已发现且有可行区域的目标。覆盖结束后也必须处理所有剩余已发现目标。unknown频道没有位置，不能用未来清除坐标入队。

路径模型：

\[
\min_\pi\left[\|x-v_{\pi_1}\|+\sum_{i=1}^{n-1}\|v_{\pi_i}-v_{\pi_{i+1}}\|\right].
\]

不要求回到起点。默认候选节点≤10时采用Held–Karp精确开放TSP：

\[
F(\{i\},i)=d(x,i),\qquad
F(S,i)=\min_{j\in S\setminus\{i\}}[F(S\setminus\{i\},j)+d(j,i)].
\]

最终取min_i F(V,i)，复杂度O(n²2ⁿ)。超过10节点时使用最近邻初始化＋2-opt反转子路径改进。`legacy`末段模式始终使用2-opt。

每轮只执行首节点任务：一个覆盖点的批量扫描，或一个目标的定位清除子任务；得到真实结果后重新构造节点、排路。不是每次单独measure都重做全局规划。局部定位则随每次测向更新。

**当前优化器直接最小化估计移动距离，不是上述全部时间项的精确期望最小化。即使本轮TSP精确，整局含未知发现与未来补测的决策仍是启发式。**

## 7. 末段粗定位补测

旧版150m入队门槛会造成“明知存在，却在覆盖完成前不参与排路”。默认`probe`规则：剩余覆盖点≤2，若首选目标ρ_j>150m且未触发过附加补测，构造：

\[
Q_\pm=c_j\pm150v,
\]

v为该频道最近测向方向的单位垂线。设预测下一节点为B，从两点中选择使\(d(x,Q)+d(Q,B)\)最小者，在该位置共享扫描后立即重排。每频道最多触发一次这类附加补测，之后回到常规定位流程，防止循环。

这里“150m入队阈值”和“150m侧向偏移”是两个不同参数用途。侧向点不是保证清除点，也不保证接收；其价值需要由真实观测检验。

P5实际复盘曾显示：C3尚未发现，C7/C11/C14分别有约587/576/359m不确定半径，旧规划只剩P6、C18。因此问题主要是候选集合不完整，单换精确TSP不能解决。新规则会让已发现的粗定位源先参加规划，但仍不能预知C3。

## 8. 局部定位、安全清除与near

常规定位：仅有一次方向时，复用问题二后验网格与期望直径增益选择第二测站；后续通常在当前最小覆盖圆中心复测。首次后验网格为空时有750u±600v解析候选兜底。一次局部定位调用最多循环补测6次，这不是整个频道在全局过程中的总测向上限。

- 若ρ_j≤19.8m，在c_j执行清除；真实目标属于U_j时，其到c_j距离≤19.8<20m。
- 若measure返回near，立即在当前位置clear，不再等待交会条件。near只在指定频道、指定测向位置触发，不是沿移动路径自动报警。
- 不需要为触发near而追求5m精度；已满足20m清除条件时直接clear更合适。
- 常规定位未完成或清除失败时，用28m方格中心覆盖剩余区域并按蛇形路线尝试。单元内任一点距中心≤28√2/2≈19.80m。

安全论证依赖误差界与可行区域正确；数值近似、测量量化和异常结果仍需兜底。状态层用当前位置的清除任务表示near，不代表已知目标精确坐标。兜底仍无法解决则报错并保存日志，不伪报完成。

## 9. 完整执行流程

```text
进入模拟器，初始化20频道与覆盖骨架
循环（正常至所有频道cleared/absent；实现上最多100轮）
  构造：剩余覆盖点 + 当前允许入队的目标中心
  ≤10点用精确开放TSP，否则最近邻+2-opt
  取路线首节点
    覆盖点：批量扫描未知频道，顺带复测已发现频道
    粗目标：若符合末段补测条件，侧向共享补测后重排
    其他目标：局部定位 → near/19.8m条件清除 → 必要时网格兜底
              清除位置补测其他已发现频道
  更新真实状态，重新规划
全部频道cleared或absent → 退出、保存结果
```

## 10. 策略与当前参数

| 策略 | 定位 |
|---|---|
| `integrated_bearing_tour` | 当前推荐：共享交会、联合巡回、小规模精确排路、末段补测 |
| `cooperative_bearing_tour` | 对照：覆盖站共享测向，覆盖后用Held–Karp滚动访问目标 |
| `robust_polygon_rolling` | 保守基线：1200m六边形，同站批扫，按插入代价滚动定位清除 |
| `belief_mpc` | 概率对照：联合粒子、覆盖参数搜索、有限深度beam search；并非当前主策略 |

| 主策略参数 | 默认 |
|---|---:|
| 六边形半径 / 是否扫原点 | 1150m / 是 |
| 常规目标入队半径 | 150m |
| 安全清除半径 | 19.8m |
| 末段模式 / 频道顺序 | probe / legacy |
| 末段启用条件 / 侧向偏移 | 剩余覆盖点≤2 / 150m |
| 精确TSP节点上限 | 10 |
| 常规局部补测循环上限 | 6 |
| 网格兜底步长 | 28m |

## 11. 已有验证与结论

不同批次的场景集、版本不同，不能跨表直接相减。

- **共享交会与联合路线**：早期26场景中，联合巡回平均3736.83s，上版滚动插入5524.50s；全部源清除。该版本尚未加入本稿末段probe优化。
- **半径/频道消融**：seed=0～59，4半径×2顺序，共480次。旧末段规则下，1200/1175/1150/1130m的平均时间分别3800.28/3752.47/3699.96/3701.27s。1150m兼顾时间与覆盖余量；51局更快、9局更慢。交替扫描平均多约2s，未设为默认。
- **末段规则消融**：相同120场景、1150m、固定策略种子1。legacy/exact/probe均值分别3717.74/3697.15/3528.87s；三组均全清、零失败清除。probe中83局更快，平均降约5.08%，非每局改善。
- **四份实际演练**：高度匹配明文日志中，CW3X为16源3642.85s、EJYF为13源3649.78s、K4CD为15源3793.24s、3XPZ为11源3497.19s。第一局1150m/旧末段，后三局1130m/probe，不能直接归因于版本变化。jlog载荷加密，未解密核验；实际日志不能回答未执行路径会得到什么观测。
- 19项非绘图断言通过；历史测试环境缺少完整pytest/绘图依赖时使用直接调用测试函数，不能表述为完整测试套件通过。

少源案例可能有更高T/N，因为固定覆盖成本分摊到更少目标、待确认不存在的频道更多。T/N与“发现至清除的平均延迟”不同；源数量与空间布局同时变化，不能仅凭分组均值证明因果。

## 12. 待验证方向（尚未并入当前策略）

1. **共享补测筛选**：结合接收半径历史约束、预计交会增益和6s左右测向/换频成本，减少低收益检测；不删除未知频道保底扫描。
2. **定位停滞判断**：多次测向后最小覆盖圆收缩很小时，跳过相近视角或选择低绕行侧向站。半径不变不等于整个区域无变化。
3. **多目标共同选站**：用一个测点对多个频道和未知覆盖区域的总收益评分，替代只围绕单目标构造两点。
4. **完整任务前瞻**：把补测→清除→下一节点的成本纳入两步规划；不能把长等待自动视为浪费，也不能假设精确当前TSP就是最优在线任务。
5. **少源收尾优化**：清除点选择性扫描未知频道，只有连续覆盖证书成立才能剪除原覆盖节点；不能因已清11或13源而直接结束。

## 13. 实现入口与复现

核心文件：`problems/problem3/strategies.py`（调度）、`shared.py`（状态/接口/定位/兜底）、`coverage.py`（覆盖证明/路径）、`belief.py`（粒子）、`config/problem3.yaml`（配置）。`run.py`优先导入本目录src，避免虚拟环境旧安装覆盖新版。

在当前项目目录执行，沿用实际模拟器队号、地址和端口：

```powershell
python run.py problem3 --strategy integrated_bearing_tour --host 127.0.0.1 --port 2026 --robot-id $env:CUMCM_ROBOT_ID
# 可选对照参数：--endgame legacy / exact / probe
# --tour-radius 1130 / 1150 / 1200
# --channel-order legacy / alternating
```

`--seed`是策略随机种子，不更改外部模拟器场景。阶段不同的历史试验应按各脚本固定参数复现：`scripts/benchmark_radius_channels.py`（半径/频道），`scripts/benchmark_endgame.py 0`与`60`（末段）。日志诊断为`audit_p5.py`、`audit_four_practices.py`；详细结果在`res/tables/problem3/`，实际动作日志在`res/logs/problem3/`。更完整的推导与历史过程见《问题三思路与具体方法.md》，四局实测诊断见`res/tables/problem3/四份演练日志分析.md`。
