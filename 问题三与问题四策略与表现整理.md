# 问题三、问题四策略与表现整理（速度 / 精度）

> 本文以 `README.md` 为主干，汇总 `problems/`、`config/`、`tests/` 与
> `res/tables/` 中的实测数据，统一整理问题三、问题四各策略的做法、速度表现和
> 精度（可靠性）表现。文中所有时间均为**虚拟任务时间**，不是程序墙钟时间。

## 0. 数据口径与前提

在引用任何数字前先约定口径，否则不同表格不可直接比较。

| 项目 | 说明 |
|---|---|
| 时间含义 | 官方模拟器规则下的虚拟时间：`T = L/5 + 5N_m + N_s + 5N_c + 3N_f`（移动 5 m/s、测向 5 s、换频 1 s、成功清除 5 s、失败清除 3 s） |
| 数据来源 | 本地 `SimulatorEngine` 配对实验（`res/tables/problem3`、`res/tables/problem4`）与四份官方演练明文日志（`res/tables/problem3/四份演练日志分析.md`） |
| 是否读真值 | 策略运行期间只接收动作返回，不读 `res/simulator/` 真值；真值仅用于事后统计清除率与轨迹复盘 |
| 场景可比性 | 不同批次的场景集、策略版本不同，**不能跨表相减**，只在同一张表内比较 |
| 精度含义 | 问题三指“频道是否全部判明 + 清除是否成功 + 定位不确定半径是否满足清除判据”；问题四指“完成率、全源清除率、清除位置误差” |
| 主要图 | `res/figures/{png,pdf}/pic3/`、`pic4/`（论文图）与 `problem3|problem4/<run-id>/`（单局轨迹、耗时、清除细节） |

两点额外说明：

- 工作区中原来的《问题三思路与具体方法.md》《问题三实现与算法模型压缩稿.md》
  在当前工作树已被删除（`git status` 显示为 `D`），本文按当前目录的实际内容重新整理，
  不依赖这两份文件的表述。
- 官方演练的 `.jlog` 为 AES-GCM 加密封装。四份演练报告用“同队号、结束/导出时间差
  5～10 ms”的明文日志重建状态，属**高度吻合而非载荷校验**，不能夸大为已解密核验。

---

## 1. 问题三：策略与表现

### 1.1 可用策略一览

| 策略名 | 调度思想 | 定位 |
|---|---|---|
| `robust_polygon_rolling` | 原点＋半径 1200 m 正六边形保证覆盖；同站批量扫描；按插入额外时间阈值滚动定位；末端 Held–Karp；28 m 网格兜底 | 保守可证明基线 |
| `belief_mpc` | 每频道 480 个“存在—位置—接收半径”粒子；覆盖多边形参数搜索（6～9 边、多半径、旋转、是否扫原点）；深度 3 / 宽度 20 Beam Search | 概率优化对照 |
| `cooperative_bearing_tour` | 覆盖点共享测向，覆盖完成后用 Held–Karp 巡回清除 | 对照方案 |
| `integrated_bearing_tour` | 覆盖节点与已定位目标共同参加最近邻＋2-opt 开放巡回，覆盖与清除交替执行 | 上一代推荐 |
| `distance_optimized_bearing_tour` | 同上，联合任务 >10 时改用多起点构造＋2-opt＋单点重插入 | 距离优先实验 |
| `route_aligned_bearing_tour` | 在联合巡回上增加“在线选角”：一个多边形对称周期内比较 12 个等价朝向，评分含后验可接收概率与交会角 $\sin^2$ | 实验变体 |
| `safe_clear_route_aligned_tour` | 利用定位圆安全余量：清除点可在圆心周围 $19.8-\rho$ m 内移动，最小化“当前—清除点—下一任务”折线 | 实验变体 |
| `dynamic_coverage_route_aligned_tour` | 利用题设“源至多 16 个”：已发现/已定位/已清除频道达 16 个时提前结束剩余全覆盖 | 实验变体 |

所有策略共用同一套底层规则：局部交会定位、`no_signal` 区域排除、最小覆盖圆、
19.8 m 安全清除判据和 28 m 网格兜底，差异只在**调度顺序、覆盖骨架和选点打分**。

### 1.2 速度表现

#### 1.2.1 同种子三种主策略对比（seed 0–9，10 局）

数据：`res/tables/problem3/strategy_time_percent_comparison.json`。

| 策略 | 平均总时间/s | 移动 | 测向 | 换频 | 清除/其他 |
|---|---:|---:|---:|---:|---:|
| `robust_polygon_rolling` | 5907.35 | 89.09% | 8.24% | 1.46% | 1.21% |
| `belief_mpc` | 5278.54 | 87.26% | 9.55% | 1.84% | 1.35% |
| `integrated_bearing_tour` | **3556.03** | 73.49% | 20.70% | 3.80% | 2.01% |

移动始终是主要成本（73%～89%）。联合巡回把“专程跑一趟定位”改成“一次移动服务多个
频道”，移动占比下降、测向占比上升，总时间比滚动插入策略少约 40%。

#### 1.2.2 联合巡回的逐级优化链（配对 120 局，seed 0–119）

数据：`route_aligned_bearing_tour_comparison_0_120.json`、
`route_aligned_bearing_tour_n7_r1050_comparison_0_120.json`、
`safe_clear_route_aligned_tour_comparison_0_120.json`、
`dynamic_coverage_route_aligned_tour_comparison_0_120.json`、`endgame_summary.json`。

| 改进步骤 | 对照基线 | 平均节省时间 | 平均节省路程 | 更快/相同/更慢 |
|---|---|---:|---:|---:|
| 在线选角（6 边 1150 m） | `integrated_bearing_tour` | 23.36 s（0.662%） | 105.36 m（0.815%） | 62 / 7 / 51 |
| 覆盖多边形改 7 边 1050 m | `integrated_bearing_tour` | 48.80 s（1.383%） | 435.46 m（3.369%） | 73 / 0 / 47 |
| `safe_clear` 安全余量内挪清除点 | `route_aligned`(7 边 1050) | 13.73 s（0.395%） | 57.11 m（0.457%） | 90 / 0 / 30 |
| `dynamic_coverage` 提前收尾 | `safe_clear` | 16.50 s（0.476%） | 33.80 m（0.272%） | 20 / 100 / 0 |
| 末段 `probe` 取代 `legacy` | `legacy` 末段 | 188.87 s（5.08%） | — | 83 局更快（其余不更快） |

- 七边形 1050 m 的选参很敏感：`1025/1050/1075 m` 的 5 m 步长细化以 1050 m 最优；
  排除用于选参的前 30 局后，留出集 seed 30–119 仍少走 379.39 m（2.952%）、
  省 33.70 s（0.960%）。
- 末段 `probe` 的收益最大但**非每局单调改善**；`exact` 只精确化当前坐标路线，
  均值略好于 `legacy`，但最慢局反而更差（5309.71 s）。
- `distance_optimized_bearing_tour` 在 6 边 1150 m 上作距离实验；正式推荐仍是
  `route_aligned_bearing_tour`（现默认 7 边 1050 m）。

#### 1.2.3 早期版本迭代（说明收益来源）

| 实验 | 局数 | 修改前均值/s | 修改后均值/s | 降幅 |
|---|---:|---:|---:|---:|
| 任务调度顺序修订 | 6 | 8656.77 | 5667.90 | 34.5% |
| 共享交会＋联合巡回 | 26 | 5524.50 | 3736.83 | 32.4%（26 局全部更快） |

早期 26 局中联合巡回全部完成频道判定与清除，时间范围 2844.83～4820.52 s。

#### 1.2.4 覆盖半径与频道顺序消融（60 场景 × 8 组 = 480 次）

数据：`radius_channel_summary.csv`、`radius_channel_ablation.json`。

| 半径/m | 原顺序均值/s | 交替顺序均值/s | 标准差(原)/s | 最慢/s |
|---:|---:|---:|---:|---:|
| 1200 | 3800.28 | 3802.20 | 489.82 | 4891.47 |
| 1175 | 3752.47 | 3754.37 | 469.36 | 4874.08 |
| **1150** | **3699.96** | 3701.93 | 478.36 | 4768.21 |
| 1130 | 3701.27 | 3703.52 | 474.93 | 4795.44 |

理论覆盖下限约 1122.956 m；1130 m 最坏距离 996.950 m（余量 ~3.05 m），
1150 m 最坏距离 988.511 m（余量 ~11.49 m）。选 1150 m 兼顾时间与余量。
交替升降序换频平均多约 2 s（移动距离完全相同，只多花换频），故未设为默认。

#### 1.2.5 四份官方演练

| 案例 | 源数 | 总时间/s | 每源时间/s | 路程/km | 移动占比 | 测向 | 换频 | 配置 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 3XPZ | 11 | 3497.19 | 317.93 | 12.99 | 74.3% | 142 | 134 | 1130 m / probe |
| CW3X | 16 | 3642.85 | 227.68 | 13.37 | 73.4% | 150 | 139 | 1150 m / legacy |
| EJYF | 13 | 3649.78 | 280.75 | 13.78 | 75.5% | 140 | 129 | 1130 m / probe |
| K4CD | 15 | 3793.24 | 252.88 | 13.97 | 73.7% | 156 | 144 | 1130 m / probe |

四局源数、配置都不同，**不能用四局时间差估计算法收益**；它们只用于核对量级：
问题三主策略在 11～16 源的实战口径下约 3.5～3.8 ks，单源约 228～318 s。
“每源时间 = 总时间 / 成功清除源数”，与“发现到清除的平均延迟”不是同一指标。

### 1.3 精度与可靠性表现

问题三的精度由几何判据保证，而不是靠统计调参：

| 机制 | 作用 |
|---|---|
| 保证覆盖骨架 | 原点＋正多边形（默认 7 边 1050 m）覆盖半径 1000 m 最坏接收情形，覆盖判据用解析最大距离＋20 m 网格复核 |
| 最小覆盖圆 | 每频道维护可行区域 $U_j=\bigcap W(M_i,\theta_i,1^\circ)$，其最小覆盖圆 $(c_j,\rho_j)$ 代表位置与不确定半径 |
| 清除判据 | $\rho_j\le19.8$ m 时在 $c_j$ 清除；真实目标必在 $U_j$ 内，故距 $c_j\le19.8<20$ m，留 0.2 m 余量 |
| `near` 分支 | 返回 `near`（距源 ≤5 m）时原地立即清除，不等待交会条件 |
| 网格兜底 | 常规定位未完成或清除失败时，28 m 方格中心蛇形访问，单元内最远 $28\sqrt2/2\approx19.80$ m，保证最终可清除 |
| 判定完成 | 未知频道的未覆盖集合为空才标 `absent`；全部频道 `cleared/absent` 才结束 |

实测结果：

| 实验 | 规模 | 全部频道判明 | 全源清除 | 清除失败次数 |
|---|---:|---:|---:|---:|
| 半径/频道消融 | 480 | 480/480 | 480/480 | 0 |
| 末段模式消融（legacy/exact/probe） | 360 | 360/360 | 360/360 | 0 |
| 联合巡回对照 | 26 | 26/26 | 26/26 | 0 |
| 四种变体配对（route_aligned/safe_clear/dynamic） | 120/组 | 全清 | 全清 | 0 |
| 四份官方演练 | 4 | 4/4 | 11+16+13+15 全部清除 | 0 |

也就是说，主策略的“精度”体现为**判据可证明**：清除成功率 100%，清除失败由兜底消化，
不存在“清除位置超差但仍算完成”的情况。

### 1.4 问题三小结

- 速度：主力 `route_aligned_bearing_tour`（7 边 1050 m）在 120 局配对中
  **约 3479.69 s**；三代主策略同种子对比从 ~5907 s（保守）→ ~5279 s（概率）
  → ~3556 s（联合巡回），收益主要来自减少移动。
- 精度：覆盖、交会、清除三层都有确定性判据，120～480 局量级全部 120/120、零清除失败；
  官方四局同样零清除失败。
- 其余策略的定位：`robust_polygon_rolling` 与 `belief_mpc` 保留为可证明基线与概率对照；
  `cooperative_*`、`distance_optimized_*` 为对照/实验变体，不改变正式默认。

---

## 2. 问题四：策略与表现

### 2.1 与问题三的差异

问题四在问题三基础上加入**定向源**：定向源只有在“接收半径内 **且** 落在其方向左右
各 90° 覆盖区内”才收到信号，否则即使更近也可能 `no_signal`。因此问题三“单点
`no_signal` 即可做圆域排除”的假设失效，问题四的策略状态（`Problem4State`）显式
禁用了该排除，只保留保证格扫描后的严格否定。

### 2.2 保证发现几何

| 方案 | 结构 | 保证依据 |
|---|---|---|
| 同心环（默认） | 中心 + 半径 1000 m 内环 12 点 + 外环 12 点，共 **25 站** | 每个剖分三角形最长边 ≤ 最低接收半径 1000 m；外环正十二边形外切目标圆，任意源附近测站凸包包含源位置，半平面朝向无论怎么转至少一站可收 |
| 三角格（对照） | 边长 1000 m 三角格，半行高平移后 27 点 | 边长不超过最低接收半径，无限三角格剖分保证任意源到最近格点 ≤1000 m |

代码用连续解析判据 `validate_directional_lattice` 做覆盖审计，不通过则直接报错，
不会静默跑一个不保证发现的路线。

### 2.3 策略一览

| 策略名 | 做法 | 定位 |
|---|---|---|
| `guaranteed_directional_lattice` | 默认实现：同心环/三角格保证扫描 → 途中机会式交会 → 顺路插入安全清除 → 末段统一清除 | 正式默认 |
| `optimized_guaranteed_lattice` | 在默认实现上改进末段（清除/收尾顺序） | 默认的优化版 |
| `concentric_ring_joint` | 同心环保证扫描＋联合定位清除（对照脚本使用的变体） | 基准对照 |
| `concentric_ring_no_insertion` | 同心环但**不做**顺路清除插入 | 消融对照 |
| `shifted_triangular_lattice` | 三角格实现 | 消融对照 |
| `legacy_outer_probe_fast` | 父项目七边形路线＋外侧六点补测＋安全网格兜底 | 研究“补全原方法要付多少时间” |
| `problem4_fast` | 忠实保留父项目高速逻辑：七边形 1000 m → 外侧六点 2246 m → 有限次主动定位，不做密集网格兜底 | 明确接受漏检风险时追求短耗时 |

### 2.4 速度表现

#### 2.4.1 同种子策略对比（seed 0–9，10 局）

数据：`res/tables/problem4/problem4_strategy_comparison.json`。

| 策略 | 平均总时间/s | 中位数/s | P90/s | 平均路程/m | 平均测向 | 平均清除失败 |
|---|---:|---:|---:|---:|---:|---:|
| `concentric_ring_joint` | **7741.60** | 7739.63 | 8240.68 | 27787.99 | 354.6 | 4.4 |
| `concentric_ring_no_insertion` | 8284.78 | 8216.31 | 8915.35 | 30514.90 | 354.2 | 4.4 |
| `shifted_triangular_lattice` | 10087.95 | 10061.55 | 10510.37 | 38615.73 | 386.3 | 3.7 |
| `problem4_fast` | 6713.31 | 6563.27 | 7980.20 | 30081.55 | 112.0 | 0 |
| `legacy_outer_probe_fast` | 13540.54 | 13667.17 | 15457.63 | 58622.21 | 188.1 | 222.1 |

顺路清除插入把同心环从 8284.78 s 降到 7741.60 s（-6.6%），而同心环比三角格少约
2346 s（-23.3%）且路程少约 10.8 km。

#### 2.4.2 大样本复核（seed 0–99，100 局）

数据：`optimized_guaranteed_lattice_audit_0_100.json`、
`concentric_ring_joint_audit_0_100.json`。

| 策略 | 平均总时间/s | 中位数/s | P90/s | 最短/最长/s | 平均路程/m | 移动/测向/换频/清除 占比 |
|---|---:|---:|---:|---:|---:|---|
| `optimized_guaranteed_lattice` | 7560.39 | 7532.05 | 8347.75 | 6363.0 / 9544.5 | 27015.05 | 71.5% / 23.0% / 4.2% / 1.3% |
| `concentric_ring_joint` | 7548.80 | 7457.15 | 8344.46 | 6358.1 / 9572.4 | 27145.49 | 71.9% / 22.8% / 4.2% / 1.1% |

问题四约 7.5 ks 量级，明显高于问题三（~3.5 ks），代价来自**为定向可发现性而增加的
25 站保证扫描**（测向次数 344～348 次，约为问题三的 2.4 倍）。

#### 2.4.3 发现路线的解析动作成本估计（非模拟）

数据：`candidate_strategy_time_percent_model.json`，只比较动作成本，不代表检出率。

| 候选路线 | 站数 | 路程/m | 估计总动作时间/s | 说明 |
|---|---:|---:|---:|---|
| 七边形直接复用 | 8 | 6516.94 | 2335.39 | 无定向保证 |
| 七边形＋外侧补测 | 14 | 17139.85 | 5173.97 | 经验方案 |
| 三角网格扫描 | 29 | 27361.63 | 9003.33 | 保证方案 |

该表说明：**速度优势必须和发现保证一起看**，无保证路线的动作成本最低，
但会漏掉定向源。

### 2.5 精度与可靠性表现

问题四的“精度”用三个口径衡量：完成率（程序自己判明全部频道）、全源清除率
（对真值）、平均源清除比例。数据来自 `benchmark_problem4.py`。

| 策略 | 完成率 | 全源清除率 | 平均源清除比例 | 100 局全部清除源数 |
|---|---:|---:|---:|---:|
| `optimized_guaranteed_lattice`（100 局） | 100% | 100% | 100% | 1339 / 1339 |
| `concentric_ring_joint`（100 局） | 100% | 100% | — | 1339 / 1339（清除数=源数） |
| `legacy_outer_probe_fast`（10 局） | 0% | 90% | 99.33% | 平均 222.1 次清除失败 |
| `problem4_fast`（10 局） | 0% | 0% | 51.0% | 0 次清除失败（但根本没找到全部源） |

关键对比结论（README 已强调，这里复核原始数据）：

- `problem4_fast` 平均 6713 s 看似最快，但**全源清除率 0%**，平均只清除 50.6%～
  52.8% 的源；它的“快”来自不完备的阴性裁剪和提前停止。
- 同一路线追加安全兜底（`legacy_outer_probe_fast`）后全源清除率升到 90%，
  但平均耗时涨到 13541 s，并有大量失败清除（平均 222.1 次）——说明补全原方法
  的时间代价极高。
- 默认同心环路线以 ~7742 s 换来 100% 完成率与 100% 全源清除率，是本问题的正确取舍。

单局精度核对（seed 0，`guaranteed_directional_lattice` 单次运行）：

| 指标 | 数值 |
|---|---|
| 全部频道判明 | 是 |
| 清除比例 | 15/15 = 100% |
| 总虚拟时间 | 7579.08 s |
| 平均“发现→清除”延迟 | 4154.34 s |
| 平均总时间/源 | 505.27 s |
| 清除点相对真值误差 | 均值 9.30 m，最大 14.01 m，最小 3.55 m |
| 清除失败次数 | 0 |

清除点误差均值约 9.3 m、最大 14.0 m，均落在题面 20 m 清除半径内且留有工程余量，
与问题三使用的 19.8 m 安全判据一致。

### 2.6 问题四小结

- 速度：默认保证策略约 **7.5～7.7 ks**（100 局量级），其中移动占 ~72%、测向占 ~23%；
  顺路清除插入相比不做插入少约 6.6%。
- 精度：保证格覆盖审计 + 19.8 m 安全清除，100 局 **1339/1339 源全部清除、完成率 100%**，
  单局清除位置误差均值 9.3 m。
- 取舍：父项目高速策略 6.7 ks 但全源清除率 0%；补兜底后 13.5 ks 换 90%。
  正式测试使用默认同心环策略，不切换高速路线。

---

## 3. 横向结论与复现命令

### 3.1 一句话对比

| 维度 | 问题三 | 问题四 |
|---|---|---|
| 主要难点 | 未知源数量下的覆盖与联合调度 | 定向源的“收得到才算发现” |
| 默认策略 | `route_aligned_bearing_tour` | `guaranteed_directional_lattice` / `optimized_*` |
| 典型耗时 | ~3.5 ks（11～16 源） | ~7.5 ks |
| 主导成本 | 移动 ~73% | 移动 ~72%，测向次数约为问题三 2.4 倍 |
| 可靠性口径 | 全部判明＋全源清除＋零清除失败 | 完成率 100%＋全源清除率 100% |
| 速度与精度取舍 | 变体优化均为“同精度下省时间” | 高速路线以牺牲清除率为代价，弃用 |

### 3.2 复现命令

问题三（本地联调需先启动模拟器，见 README）：

```bash
python run.py problem3 --strategy route_aligned_bearing_tour --host 127.0.0.1 --port 2026 --robot-id demo
python scripts/benchmark_distance_tour.py --candidate route_aligned_bearing_tour --start 0 --count 120
python scripts/benchmark_endgame.py 0
python scripts/benchmark_endgame.py 60
```

问题四：

```bash
python main.py simulator --problem 4 --seed 0 --robot-id demo --port 2026
python run.py problem4 --host 127.0.0.1 --port 2026 --robot-id demo --seed 0 \
  --truth-file res/simulator/problem4_seed0.json
python scripts/benchmark_problem4.py --seed-start 0 --seed-count 100 --only concentric_ring_joint
```

绘图：`res/figures/{png,pdf}/pic3/`、`pic4/` 为论文图；
`res/figures/{png,pdf}/problem3|problem4/<run-id>/` 为单局轨迹、清除细节与耗时图。

### 3.3 使用这些数字时的注意事项

1. 不同批次场景集与版本不同，**只在同一张表内做减法**。
2. 所有时间为虚拟时间；程序墙钟时间与绘图依赖无关，绘图异常会记录在运行日志里。
3. 官方演练 `jlog` 未解密核验，四局结论只用于量级核对，不能作为算法收益的证据。
4. 早期测试环境缺少完整 pytest/绘图依赖，部分断言以直接调用测试函数方式通过，
   不能表述为“完整测试套件通过”。
5. 少源场景的“每源时间”更高是结构性的（固定覆盖成本摊到更少目标、更多频道需证明不存在），
   不能据此断定算法在少源时更差。
