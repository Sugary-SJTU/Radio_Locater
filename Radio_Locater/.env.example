# 问题 3、4 连接模拟器时读取以下环境变量。
# 本文件只提供字段示例，不会被 Python 自动加载；不要在仓库中写入真实参赛队号。
CUMCM_ROBOT_ID=<参赛队号>
# 模拟器默认只监听本机回环地址；若在界面修改端口，只修改这里的端口部分。
CUMCM_BASE_URL=http://127.0.0.1:2026
# 该值是一次 HTTP 请求的现实等待时间，不是 /measure 的 5 秒虚拟动作耗时。
CUMCM_HTTP_TIMEOUT_S=5
